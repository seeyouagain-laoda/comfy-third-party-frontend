# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.0] - 2026-09-25

首个公开版本。

### 功能

- **文生图**：中文描述直接出图，实时回显采样进度、随机种子、分辨率。
- **图生图 / 多图编辑**：支持单图编辑、多参考图（可在提示词里按序号引用）、批量处理整个文件夹。
- **AI 视频（MiniMax-H3）**：内置 H3 提示词模板（首尾帧定义 / 分镜规划 / 视觉风格 / 镜头运动），支持「图 + 文 → 视频」参考模式，输出带音轨的短视频。
- **提示词优化**：一句大白话 → 专业绘图 / 视频提示词，可接本地模型或任意 OpenAI 兼容 API。
- **大模型对话**：支持拖拽图片 / 文本附件，长对话自动压缩上下文。

### 工程

- 后端纯 Python 标准库实现，无第三方框架依赖；前端单 HTML 文件，无构建步骤。
- 引擎显存互斥：多个引擎（ComfyUI / LLM）自动仲裁，同一时刻只跑一个。
- 页面心跳 + 自动退出：关掉页面后后端自动回收，不留僵尸进程。
- 启动器支持 `--tab` / `--no-browser` / `--stop`，并内置单实例检测。
- 生成物按「模块 / 模型 / 日期」自动归档。
- 日志术语中文化（`stream` → 流式、`TTFB` → 首字节 等）。

### 文档

- 附带完整《保姆级教程》（`docs/保姆级教程.md`），覆盖从装 Python 到 H3 视频全流程，含排错章节、配置字段全解、代码结构说明与实测验证记录。
